# simplefin-aggregator

A SimpleFIN Bridge server that aggregates data from one or more
SimpleFIN providers. Intended for use by personal finance apps such as
Actual Budget that do not support multiple SimpleFIN providers. This
is a work in progress; fuller setup docs land as the corresponding
functionality is built.

## Status

Implemented so far: config loading and the `claim` CLI subcommand.
