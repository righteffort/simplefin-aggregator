# sf-agg config. See README.md.

# Set bind_host to 0.0.0.0 when running under Docker.
bind_host = "127.0.0.1"
bind_port = 5026

# The URL this aggregator is reachable at from the client app. It is embedded
# in every setup token `client add` prints, so it must be ASCII.
base_url = "http://127.0.0.1:5026"

# Set to true to allow multiple entries in providers (below) to have prefix = "".
# allow_multiple_blank_prefixes = false

# The providers to aggregate, by key. Built-in keys: simplefin-bridge,
# lunchflow, redbark. See README.md for the details of customizing the prefix
# field.
[[providers]]
key = "simplefin-bridge"
prefix = ""

# [[providers]]
# key = "lunchflow"

# Providers the built-in list does not name.
#
# [[custom_providers]]
# key = "my-bridge"
# root = "https://simplefin.example.com/simplefin"
